<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow unsigned executable memory (required for some Rust binaries) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Disable library validation (allows loading of unsigned libraries) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- USB device access (required for HID/keyboard access) -->
    <key>com.apple.security.device.usb</key>
    <true/>

    <!-- Allow DYLD environment variables (may be needed for Rust runtime) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
</dict>
</plist>
